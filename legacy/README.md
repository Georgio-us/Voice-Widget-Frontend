Legacy / non-production helper files moved here to avoid deploy confusion.

- bot-logic-patch.js: manual bot integration patch example
- console-demo.js: browser console demo bootstrap script

These files are not used by the production runtime.
